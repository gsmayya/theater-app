export interface Ticket {
  mobileNumber: string;
  numberOfTickets: number;
  name: string;
  paymentMobileNumber: string;
  dateTime: string;
  itemId: string;
  bookedBy: string;
  uniqueId: string;
}